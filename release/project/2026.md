# Project Releases — 2026

Rollup only — references Frontend/Backend release versions, does not duplicate their technical detail. Dated entries (`REL-YYYY-MM-DD`), not semver: a project release is a point-in-time bundling/communication event, not an independently evolving artifact. See [Release Index](../000_index.md).

Migrated from `work_history.md` (now archived at `sdd/archive/work_history.md`) as part of the Sentinel Development Operating Model rollout, 2026-07-06.

---

## REL-2026-07-06

**Bundles:** `frontend-v1.0.0`, `backend-v1.0.0` (baseline; backend unchanged this release)

Sentinel Development Operating Model rollout: SDD drift fixes (database schema migration list, API spec gaps, stale frontend-arch example), introduced ADR area (`sdd/architecture/decisions/`, 4 backfilled ADRs), archived two orphaned root-level documents, introduced this Release structure. Also includes the `AUTH_PROVIDER=supabase` conditional-Supabase-initialization login fix and `scripts/deploy_web.sh`, deployed to Firebase Hosting earlier in the same working session.

---

## REL-2026-07-01

**Bundles:** `backend-v1.0.0`, frontend (pre-baseline — see `sdd/archive/work_history.md` for the exact pre-tag commit)

`feat/ocr` merged to `main`, reconciled with remote `develop` PR divergence, pushed to origin triggering GitHub Actions CI/CD for the backend. Frontend production build compiled with `AUTH_PROVIDER=supabase`, `USE_MOCK_DATA=false`, `SKIP_EMAIL_VERIFICATION=false` and deployed to Firebase Hosting. Database already at `b2c3d4e5f6a7` (head) — no new migration required.

- Backend (Cloud Run): `https://sentinel-backend-twyh3esabq-du.a.run.app` — HTTP 403 on guarded route (auth active, `/docs` disabled in production)
- Frontend (Firebase): `https://sentinel-mvp-eeeee.web.app` — HTTP 200

---

## REL-2026-06-30

**Bundles:** pre-baseline (see `sdd/archive/work_history.md`)

Full release workflow for the 2026-06-30 session: Alembic migration `b2c3d4e5f6a7` applied to PostgreSQL. Backend restarted (`/docs` 200, `/api/v1/dev/token` live). Frontend started with `AUTH_PROVIDER=dev`, `USE_MOCK_DATA=false`.

Session also shipped (same day, pre-dating this release structure — see `sdd/archive/work_history.md` for full per-commit detail): AI Platform schema migration and ORM evolution, AI Platform core package (handlers/registry/executor/context), incident service/schema/router updates for AI Platform, backend dev auth (validator composition + dev token endpoint — see [ADR-0001](../../sdd/architecture/decisions/ADR-0001-jwt-validator-dispatch.md)), Flutter `DevAuthRepository`, a 169-test backend suite, the `sdd/auth/` specification set, and the `sdd/workflow/` + `sdd/domain/` + `sdd/rules/` governance documents this Operating Model itself now extends.
