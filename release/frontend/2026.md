# Frontend Releases — 2026

Independent semantic version for the Flutter web app. References git tags (`frontend-vX.Y.Z`), never raw commit hashes. See [Release Index](../000_index.md).

---

## frontend-v1.0.0 — 2026-07-06

**Tag:** `frontend-v1.0.0`
**Deployed:** Firebase Hosting — `https://sentinel-mvp-eeeee.web.app`
**Built with:** `AUTH_PROVIDER=supabase`, `USE_MOCK_DATA=false`, `SKIP_EMAIL_VERIFICATION=false`, via `scripts/deploy_web.sh production`

Baseline release. Established retroactively during the Sentinel Development Operating Model rollout to anchor the versioning discipline going forward — `pubspec.yaml`'s `version: 1.0.0+1` had been declared but never tagged prior to this. This deploy included the `AUTH_PROVIDER=supabase` conditional-initialization fix (login `FormatSyntaxError` fix — see [ADR-0001](../../sdd/architecture/decisions/ADR-0001-jwt-validator-dispatch.md) for the related dev-auth dispatch design) and the new `scripts/deploy_web.sh` single-command deploy tooling.

---

*Entries before this baseline predate the git-tag convention introduced by the Sentinel Development Operating Model (2026-07-06) and are not individually re-tagged retroactively — see the archived `work_history.md` for prior frontend deploys, referenced there by commit hash as originally recorded.*
